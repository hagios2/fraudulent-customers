server:
  port: 8081

spring:
  application:
    name: fraud
  datasource:
    username: 'oteng'
    url: jdbc:postgresql://localhost:5433/fraud
    password: 'password'
    jpa:
      properties:
        hibernate:
          dialect: org.hibernate.dialect.PostgreSQLDialect
          form_sql: 'true'
      hibernate:
        ddl-auto: update
        show-sql: 'true'