server:
  port: 8080

spring:
  application:
    name: customer
  datasource:
    username: 'oteng'
    url: jdbc:postgresql://localhost:5433/customer
    password: 'password'
    jpa:
      properties:
        hibernate:
          dialect: org.hibernate.dialect.PostgreSQLDialect
          form_sql: 'true'
      hibernate:
        ddl-auto: update
        show-sql: 'true'
